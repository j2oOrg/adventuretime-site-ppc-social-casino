apiVersion: apps/v1
kind: Deployment
metadata:
  name: adventuretime-site-ppc-social
  labels:
    app: adventuretime-site-ppc-social
spec:
  replicas: 1
  selector:
    matchLabels:
      app: adventuretime-site-ppc-social
  template:
    metadata:
      labels:
        app: adventuretime-site-ppc-social
    spec:
      containers:
        - name: adventuretime-site-ppc-social
          image: IMAGE_PLACEHOLDER
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: adventuretime-site-ppc-social
  labels:
    app: adventuretime-site-ppc-social
spec:
  type: LoadBalancer
  selector:
    app: adventuretime-site-ppc-social
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  loadBalancerIP: LOAD_BALANCER_IP_PLACEHOLDER